# Фаза E — второй город, интеграция, осторожный социальный слой

## Второй город (уже в демо)

В [../../data/taste-compass/places.json](../../data/taste-compass/places.json) есть два `cityId`: `berlin_demo` и `tokyo_demo`. Добавление нового города:

1. Запись в массив `cities`.  
2. Карточки в `places` с тем же `cityId` и тегами из опроса.

## Одна интеграция (например Spotify)

В этом репозитории уже есть **Spotify OAuth** и топ-артисты ([../../app.js](../../app.js)). Возможный следующий шаг — **не обязательный для TasteCompass MVP**: подмешивать теги жанров из Spotify к `musicTags` профиля. Делать только после того, как ручной опрос + каталог стабильны.

## Осторожный социальный слой

Не дублировать Tinder. Варианты позже:

- «Кто ещё сохранил это событие» (без чата до согласия).  
- Чат только после взаимного интереса на **конкретном ивенте**.  
- Модерация и политика — отдельные документы (Trust & Safety).

## Критерий

Новый город или интеграция не ломают узкий сценарий «культурный компас» для первого города.
